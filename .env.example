# API Configuration
# 개발 환경: Vite 프록시 사용 (vite.config.ts에서 /api -> http://localhost:8080/api 프록시)
# VITE_API_BASE_URL=/api

# 프로덕션 환경: 실제 서버 URL 사용
# VITE_API_BASE_URL=https://api.production.com/api

VITE_API_TIMEOUT=10000

# WebSocket Configuration
# 프로토콜(ws:// 또는 wss://)은 포함하지 마세요. 코드에서 자동으로 추가됩니다.
# 개발 환경: 로컬 서버
# VITE_WS_BASE_URL=localhost:8080

# 프로덕션 환경: 실제 서버 IP 또는 도메인
VITE_WS_BASE_URL=16.184.24.121:8080
# 또는 도메인 사용
# VITE_WS_BASE_URL=production.com:8080
